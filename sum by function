#include <stdio.h>

void sum(int begin,int end);

int main()
{
	sum(1,10); // int sum(int,int)
	sum(20,30);
	sum(30,40);
	
	return 0;
}

void sum(int begin,int end)
{
	int i;
	int sum=0;
	for(i=begin;i<=end;i++){
		sum+=i;
	}
	printf("sum of %d to %d is %d\n",begin,end,sum);
